version: '3'
services:
  router:
    restart: always
    build: "./docker/router"
    ports:
      - "443:443"
  web:
    restart: always
    build: "./docker/web"
    volumes:
      - ./website:/var/www/html
  legacyweb:
    restart: always
    build: "./docker/legacyweb"
  dns:
    build:
      context: .
      dockerfile: "./docker/zdxsv/Dockerfile"
    entrypoint:
      - /zdxsv
      - dns
      - /config.toml
    ports:
      - "53:53/udp"
  login:
    build:
      context: .
      dockerfile: "./docker/zdxsv/Dockerfile"
    volumes:
      - ./zdxsv.db:/zdxsv.db
    entrypoint:
      - /zdxsv
      - login
      - /config.toml
  lobby:
    build:
      context: .
      dockerfile: "./docker/zdxsv/Dockerfile"
    ports:
      - "8200:8200"
    volumes:
      - ./zdxsv.db:/zdxsv.db
    entrypoint:
      - /zdxsv
      - lobby
      - /config.toml
  battle:
    build:
      context: .
      dockerfile: "./docker/zdxsv/Dockerfile"
    ports:
      - "8210:8210"
      - "8210:8210/udp"
    entrypoint:
      - /zdxsv
      - battle
      - /config.toml
  status:
    build:
      context: .
      dockerfile: "./docker/zdxsv/Dockerfile"
    entrypoint:
      - /zdxsv
      - status
      - /config.toml